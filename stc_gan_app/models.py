from django.db import models

# Create your models here.
# This modul is actually for db stuff, but I will use it for the model, since it's already
# called models.py and I will not need to use a db.

def i2i_style_transfer(input):
    # do stuff
    return input
